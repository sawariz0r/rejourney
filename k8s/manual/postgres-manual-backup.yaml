# Manual-only on-demand CNPG backup request. Copy and rename before use so a
# fresh Backup object is created for each operation.

apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: postgres-manual-backup-template
  namespace: rejourney
spec:
  method: barmanObjectStore
  cluster:
    name: postgres
